<p id="notice"><%= notice %></p>

<h1>Manual Control</h1>

<% for control in @controls %>

<table valign="middle">

  <tr>
    <th> <h3><%= control.name %></h3> </th>
    <td>
      <label class="switch">
        <input type="checkbox" id="toggle" name="<%= control.id %>"<%="checked" if control.automated %> >
        <div class="slider round"></div>
      </label>
    </td>
  </tr>

</table>

<% end %>

<br>
